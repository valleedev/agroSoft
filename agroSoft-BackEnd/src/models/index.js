const User = require('./User.model');
const Collaborator = require('./Collaborator.model');
const Tool = require('./Tools.model')
const Seeding = require('./Seeding.model')


module.exports = {
    User,
    Collaborator,
    Tool,
    Seeding
}