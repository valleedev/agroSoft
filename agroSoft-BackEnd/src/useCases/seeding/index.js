const {createSeedingUseCase} = require('./createSeeding.usecase');
const {getSeedingsUseCase} = require('./getAllSeedings.usecase');

module.exports = {
    createSeedingUseCase,
    getSeedingsUseCase
}